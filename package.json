{
  "name": "openclaw-workspace",
  "private": true,
  "scripts": {
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "lint:admin": "cd openclaw-admin && oxlint --config .oxlintrc.json src/"
  },
  "devDependencies": {
    "oxlint": "^1.49.0"
  }
}
