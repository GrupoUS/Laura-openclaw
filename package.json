{
  "name": "openclaw-workspace",
  "private": true,
  "scripts": {
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "lint:admin": "cd dashboard && oxlint --config .oxlintrc.json src/"
  },
  "devDependencies": {
    "oxlint": "^1.49.0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  }
}
